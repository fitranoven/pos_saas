{
  "name": "firstan-pos",
  "version": "1.0.0",
  "description": "Enterprise-grade AI-powered SaaS Point of Sale",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npx concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}
